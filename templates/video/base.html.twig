
{% extends 'base.html.twig' %}

{% block title %}Video{% endblock %}

        {% block page_header %}
            <div class="page-header" id="banner">
                {{ component('menu', {
                    type: PAGE_MENU,
                    caller: _self,
                    options: {
                        video: video
                    }
                }
                ) }}
            </div>
        {% endblock %}
