{# <div class="hit-category">{{ hit._highlightResult.category.value }} #}
{#    <span class="hit-value">for ${{ hit.value }}</span> #}
{# </div> #}

{# <div class="hit-question">{{ hit._highlightResult.question.value }}</div> #}

{% set _sc = 'reveal' %}

{# <div class="row row-cols-1 row-cols-md-2 row-cols-lg-3 g-4"> #}
{#    <div class="col"> #}
<div class="card h-100">
    <div class="card-header d-flex justify-content-between align-items-center">
        <span class="fw-bold">{{ hit._highlightResult.title.value }}</span>
    </div>

    <img src="{{ hit.thumbnailUrl }}" />

    <div class="card-body">
        <div class="blockquote">
            {{ hit._highlightResult.description.value }}
        </div>
    </div>

    <div class="card-footer clearfix xxd-flex float-end xjustify-content-end">
        <div class="float-start">
            {{ hit._highlightResult.type.value }}
        </div>
        {% if hit._rankingScore != 1 %}
            <span class="m-4">
                        score: {{ hit._rankingScore|number_format(2) }}
                </span>
        {% endif %}
{#        {{ _sc_modal }}#}
{#        <pre>{{ stimulus_action(_sc_modal, 'modal') }}#}
{#data-action="survos--meili-bundle--json#modal"#}
{#        </pre>#}
{#        <br />#}
        <button
            {{ stimulus_action(_sc_modal, 'modal') }}
            data-hit-id="{{ hit.id }}"
            class="float-end btn btn-sm btn-outline-primary">
            <span class="me-1 icon-wrapper">{{ icons.json|raw }}</span>
        </button>

    </div>
</div>


